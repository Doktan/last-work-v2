import React from 'react';
import StatusTable from './StatusTable';

const Status = (props) => {
    return(
        <div>
            <StatusTable data={[]}/>
        </div>
    );
};

export default Status;
